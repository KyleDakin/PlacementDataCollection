def generateRandomPoints(address, i, j): 
    x = i/111111
    lat = math.radians(float(address[2]))
    long = math.radians(float(address[3]))
    y = math.radians(j)
    latRadians = math.asin(math.sin(lat) * math.cos(x) + math.cos(lat) * math.sin(x) * math.cos(y))
    longRadians = long + math.atan2(math.sin(y) * math.sin(x) * math.cos(lat), math.cos(x) - math.sin(lat) * math.sin(latRadians))
    address[2] = math.degrees(latRadians)
    address[3] = math.degrees(longRadians)

    return address

	
#Takes in an address in latitude and longitude
#Calculates the distance in metres from the entered value i
#Converts lat/long into radians
#Converts j into radians as this is the number of points generated on the circle (angle between each point)
#Calculates the latitude and longitude in radians based on the distance and the angle
#Converts the address back into degrees and returns the address